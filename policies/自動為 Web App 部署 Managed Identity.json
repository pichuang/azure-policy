{
	"properties": {
		"displayName": "自動為 Web App 部署 Managed Identity",
		"description": "此原則會自動為 Web App 啟用系統指派的 Managed Identity，讓應用程式可使用受控識別安全地存取其他 Azure 服務而不需自行管理認證。如果 Web App 已設定使用者指派的 Managed Identity，原則會保留既有設定並額外啟用系統指派的 Managed Identity。",
		"policyType": "Custom",
		"mode": "Indexed",
		"metadata": {
			"category": "App Service",
			"version": "1.0.0"
		},
		"parameters": {
			"effect": {
				"type": "String",
				"metadata": {
					"displayName": "Effect",
					"description": "啟用或停用本原則的執行"
				},
				"allowedValues": [
					"Modify",
					"Disabled"
				],
				"defaultValue": "Modify"
			}
		},
		"policyRule": {
			"if": {
				"allOf": [
					{
						"field": "type",
						"equals": "Microsoft.Web/sites"
					},
					{
						"field": "kind",
						"notContains": "functionapp"
					},
					{
						"field": "kind",
						"notContains": "workflowapp"
					},
					{
						"field": "identity.type",
						"notContains": "SystemAssigned"
					}
				]
			},
			"then": {
				"effect": "[parameters('effect')]",
				"details": {
					"roleDefinitionIds": [
						"/providers/Microsoft.Authorization/roleDefinitions/de139f84-1756-47ae-9be6-808fbbe84772"
					],
					"conflictEffect": "audit",
					"operations": [
						{
							"operation": "addOrReplace",
							"field": "identity.type",
							"value": "[if(or(equals(field('identity.type'), 'UserAssigned'), equals(field('identity.type'), 'SystemAssigned, UserAssigned')), 'SystemAssigned, UserAssigned', 'SystemAssigned')]"
						}
					]
				}
			}
		}
	}
}
